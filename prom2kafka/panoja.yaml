team_configs:
  - subject_dn: "CN=poc.leanix.wcd2i.k8s.asgard.dub.aws.k8s.3stripes.net,OU=Global IT,O=adidas AG,L=Herzogenaurach,ST=Bavaria,C=DE"
    rate: 1500000
    private_tenant: 247
    relabel_configs:
      - source_labels: [leanix]
        regex: leanix
        action: keep
    kafka_forward_configs:
      - source_labels: [kafka]
        regex: true
        action: keep

global_relabels:
  - source_labels: [environment]
    regex: production
    action: keep